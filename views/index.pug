doctype html
html
    head
        title Main page
        body
            if (user)
                p Welcome, #{user.username}!

            else
                h1 Not logged in, please register or login.
                a(href="/register") Register
                a(href="/login") Log in