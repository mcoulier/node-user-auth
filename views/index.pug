extends layout

block content
    if (user)
        p Welcome, #{user.username}
        a(href="/logout") Log out

    else
        h1 Not logged in, please register or login.
        a(href="/register") Register
        br
        a(href="/login") Log in